# AntCrew — example config
# Run: python examples/04_yaml_config.py
team: dev
model: simulated          # swap for: claude | gpt-4o | ollama:llama3

# Persistent LLM cache (avoids repeated API calls during development)
# cache: .antcrew_cache.db

# Persistent project sessions (state accumulates across runs)
# project: ./my-project.json

# Sandbox for executing generated tests
# runner:
#   type: local
#   timeout: 60

# Optional: per-agent model / approval overrides
# agents:
#   pm:
#     model: claude-sonnet-4-6
#     approval_required: true
#   backend_dev:
#     model: ollama:llama3
